[theme]
primaryColor = "#1f77b4"
backgroundColor = "#ffffff"
secondaryBackgroundColor = "#f0f2f6"
textColor = "#262730"
font = "sans serif"

[server]
headless = false
enableCORS = false
enableXsrfProtection = true

[browser]
gatherUsageStats = false

# Visual Editor settings
# Note: The Visual Editor is primarily enabled through the UI settings menu
# To enable it: Click the hamburger menu (☰) → Settings → Visual Editor → Enable
