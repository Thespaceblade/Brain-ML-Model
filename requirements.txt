# Prefer CPU wheels on Streamlit Cloud / low-RAM hosts (avoids huge CUDA installs)
--extra-index-url https://download.pytorch.org/whl/cpu

streamlit>=1.28.0,<2
torch==2.2.2
torchvision==0.17.2
numpy>=1.24.0,<3
pillow>=9.5.0
